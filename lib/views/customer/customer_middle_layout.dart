import 'package:flutter/material.dart';

class CustomerMiddleLayout extends StatelessWidget {
  const CustomerMiddleLayout({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("Customer Tablet Dashboard")),
    );
  }
}