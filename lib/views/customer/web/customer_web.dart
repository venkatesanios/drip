import 'package:flutter/material.dart';

class CustomerWeb extends StatelessWidget {
  const CustomerWeb({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("Customer Web Dashboard")),
    );
  }
}