import 'package:flutter/material.dart';

class AdminMobile extends StatelessWidget {
  const AdminMobile({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("Admin Mobile Dashboard")),
    );
  }
}