import 'package:flutter/material.dart';

class AdminDesktop extends StatelessWidget {
  const AdminDesktop({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("Admin Desktop Dashboard")),
    );
  }
}