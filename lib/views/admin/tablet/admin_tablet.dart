import 'package:flutter/material.dart';

class AdminTablet extends StatelessWidget {
  const AdminTablet({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("Admin Tablet Dashboard")),
    );
  }
}