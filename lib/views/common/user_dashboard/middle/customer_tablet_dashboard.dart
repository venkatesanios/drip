import 'package:flutter/material.dart';

class CustomerTabletDashboard extends StatelessWidget {
  const CustomerTabletDashboard({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: throw UnimplementedError(),
    );
  }

}