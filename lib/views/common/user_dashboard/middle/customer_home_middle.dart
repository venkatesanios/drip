import 'package:flutter/material.dart';

class CustomerHomeMiddle extends StatelessWidget {
  const CustomerHomeMiddle({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: throw UnimplementedError(),
    );
  }

}