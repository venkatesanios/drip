import 'package:flutter/material.dart';

class AdminTabletDashboard extends StatelessWidget {
  const AdminTabletDashboard({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: throw UnimplementedError(),
    );
  }

}
