import 'package:flutter/material.dart';

class DealerWeb extends StatelessWidget {
  const DealerWeb({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("Dealer Web Dashboard")),
    );
  }
}