import 'package:flutter/material.dart';

class DealerDesktop extends StatelessWidget {
  const DealerDesktop({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("Dealer Desktop Dashboard")),
    );
  }
}