import 'package:flutter/material.dart';

class DealerMobile extends StatelessWidget {
  const DealerMobile({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("Dealer Mobile Dashboard")),
    );
  }
}