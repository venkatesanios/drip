import 'package:flutter/material.dart';

class DealerTablet extends StatelessWidget {
  const DealerTablet({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("Dealer Tablet Dashboard")),
    );
  }
}